config:
  message: Hello From Config Server
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    hostname: localhost

